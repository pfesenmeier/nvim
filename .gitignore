nushell/history.txt

# Generated by `jjui --install-lua-types` (LSP completion only).
config/jjui/types.lua
config/jjui/.luarc.json

# Synced down by Claude Code from the claude.ai account (~/.claude/skills is a
# symlink into this repo). Re-downloadable cache, not source.
config/claude/skills/synced/
