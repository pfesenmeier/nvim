
I think there is a bug in code x with symptoms y, my guess on the mechanism is z. Read all relevant code, come back with a call chain and code citations.

If you need a paragraph-long comment to justify why the workaround is OK, the code is wrong — fix the code.
